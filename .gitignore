# BOOTGEN
bootgen/*.BIN
bootgen/*.bin

# sources/
sources/**
!sources/*.json

# Ignore rootfs (maybe better to only keep the archived image?)
root
rootfs
initramfs

# build binaries and artefacts
apps/*/build/bin
apps/*/build/obj